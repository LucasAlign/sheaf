{
  "name": "bridge-workspace",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "db:migrate": "node ./scripts/migrate-postgres.mjs",
    "db:seed-demo": "node ./scripts/seed-demo-data.mjs",
    "dev": "pnpm --filter @workspace/bridge run dev",
    "typecheck": "pnpm --filter @workspace/api-server run typecheck && pnpm --filter @workspace/bridge run typecheck",
    "build": "pnpm --filter @workspace/api-server run build && PORT=${PORT:-20043} BASE_PATH=${BASE_PATH:-/} pnpm --filter @workspace/bridge run build"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}